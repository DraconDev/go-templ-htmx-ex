package templates

//go:generate templ generate
templ HomeContent() {
	<div class="max-w-4xl mx-auto">
		<div class="text-center mb-8">
			<h2 class="text-3xl font-bold text-gray-900 mb-4">Welcome to Our Startup Platform</h2>
			<p class="text-lg text-gray-600 mb-6">A modern application powered by Google OAuth authentication</p>
			
			<!-- Authentication Status -->
			<div class="mb-6">
				<button
					hx-get="/api/auth/user"
					hx-target="#auth-status"
					hx-swap="innerHTML"
					class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mr-4"
				>
					Check Login Status
				</button>
				<div id="auth-status" class="mt-2 text-sm text-gray-500">
					Click to check your authentication status
				</div>
			</div>
		</div>
		
		<div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
			<div class="bg-white rounded-lg shadow p-6">
				<h3 class="text-xl font-semibold text-gray-900 mb-3">🔐 Secure Authentication</h3>
				<p class="text-gray-600 mb-4">
					Our startup leverages Google OAuth for secure user authentication through our dedicated auth microservice.
				</p>
				<div class="space-y-2">
					<a href="/auth/google" class="block bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded text-center">
						Login with Google
					</a>
					<a href="/profile" class="block bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded text-center">
						View Profile
					</a>
				</div>
			</div>
			
			<div class="bg-white rounded-lg shadow p-6">
				<h3 class="text-xl font-semibold text-gray-900 mb-3">🚀 Microservice Architecture</h3>
				<p class="text-gray-600 mb-4">
					Built with modern microservice patterns, including a dedicated authentication service.
				</p>
				<a href="/test" class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded block text-center">
					Test Services
				</a>
			</div>
		</div>
		
		<div class="bg-white rounded-lg shadow p-6">
			<h3 class="text-xl font-semibold text-gray-900 mb-3">🛠️ Auth Service Integration</h3>
			<p class="text-gray-600 mb-4">
				This application demonstrates integration with our auth microservice, providing:
			</p>
			<ul class="list-disc list-inside text-gray-600 space-y-1">
				<li>Google OAuth 2.0 authentication</li>
				<li>JWT token management</li>
				<li>Secure session handling</li>
				<li>Real-time auth status checking</li>
			</ul>
		</div>
	</div>
}
